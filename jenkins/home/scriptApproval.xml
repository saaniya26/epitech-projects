<?xml version='1.1' encoding='UTF-8'?>
<scriptApproval plugin="script-security@1399.ve6a_66547f6e1">
  <approvedScriptHashes/>
  <approvedSignatures/>
  <aclApprovedSignatures/>
  <approvedClasspathEntries/>
  <forceSandbox>false</forceSandbox>
  <pendingScripts>
    <pendingScript>
      <context>
        <item>tools/SEED</item>
      </context>
      <script>def githubName = binding.hasVariable(&apos;GITHUB_NAME&apos;) ? GITHUB_NAME : System.getenv(&apos;GITHUB_NAME&apos;)
def displayName = binding.hasVariable(&apos;DISPLAY_NAME&apos;) ? DISPLAY_NAME : System.getenv(&apos;DISPLAY_NAME&apos;)

if (!githubName?.trim()) {
    throw new IllegalArgumentException(&apos;GITHUB_NAME is required&apos;)
}
if (!displayName?.trim()) {
    throw new IllegalArgumentException(&apos;DISPLAY_NAME is required&apos;)
}

job(displayName) {
    description(&quot;Generated by SEED for ${githubName}&quot;)

    properties {
        githubProjectUrl(&quot;https://github.com/${githubName}&quot;)
    }

    scm {
        git {
            remote {
                github(githubName, &apos;https&apos;)
            }
        }
    }

    triggers {
        scm(&apos;* * * * *&apos;)
    }

    wrappers {
        preBuildCleanup()
    }

    steps {
        shell(&apos;make fclean&apos;)
        shell(&apos;make&apos;)
        shell(&apos;make tests_run&apos;)
        shell(&apos;make clean&apos;)
    }
}
</script>
      <language>groovy</language>
    </pendingScript>
  </pendingScripts>
  <pendingSignatures/>
  <pendingClasspathEntries/>
</scriptApproval>